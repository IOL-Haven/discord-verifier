pluginManagement {
	repositories {
		mavenLocal()
		maven {
			name = 'Fabric'
			url = 'https://maven.fabricmc.net/'
		}
		mavenCentral()
		gradlePluginPortal()
	}
}

// Register the Discord4J wrapper as a subproject that can be used as part of a composite build
includeBuild "discord4j-wrapper"